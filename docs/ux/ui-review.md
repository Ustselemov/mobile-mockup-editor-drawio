# UI Review

The editor is now centered on visual construction instead of demo browsing. The left side is a real library, the board has overlays for fast action, and `Layers` is secondary. Remaining weak area: inspector density and top-bar compaction.
