# Editor Layout

- Full-screen shell with fixed top bar, central workspace, fixed status line.
- Left side split into icon rail plus library content.
- Right side split into `Inspector` and `Layers`.
- Board overlays allowed inside the central stage only.
