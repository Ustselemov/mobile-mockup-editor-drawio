# Draw.io Compatibility

## Guarantees
- JSON is primary
- supported draw.io subset round-trips through parser/serializer
- unsupported nodes are preserved as fallback nodes
- warnings are exposed in document metadata and validation flows

## Current boundary
The product is not a full draw.io clone. Compatibility remains explicit and controlled.
